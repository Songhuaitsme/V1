#### 待评审问题清单

1. 是否正式采用准时传输，不允许数据提前到达后在节点等待？**是的**
2. 传输时长应如何由数据量、任务带宽、路径容量和传播时延计算？**传输时长由路径长短、链路速率、任务量、路径容量影响给出解决方案我不太确定**
3. Soft任务超过当前时间限制后的业务后果是什么，绝对最晚开始时间如何定义？
4. None任务的最大未来预约范围是多少？**SLA约束范围内**
5. 预约窗口内没有候选时，任务立即拒绝还是进入未承诺队列？**进入未承诺队列重新尝试调度**
6. 已建立预约是否允许取消、迁移或重新规划？**当前暂不考虑，后续再设计如果能够得到较好的结果就允许**
7. 第一阶段是否接受无误差电价和绿电预测？**接受**
8. 调度器是否明确禁止使用未来尚未到达任务的信息？**明确禁止**
9. 绿电核心优化对象是覆盖率、消纳率、绝对绿电使用量还是组合目标？**组合目标，多目标优化**
10. 成本和绿电同级目标在训练阶段采用什么数学方法？**先提供给我有什么方法?**
11. 各核心指标的通过阈值和允许退化范围是多少？**提供给我解决方案**
12. WAIT反事实收益如何定义和测量？**提供给我解决方案**
    
    
    
    **[待确认]--[已确认]** 需要明确：
    * 当前 `latency_limit` 是期望开始时间还是绝对最晚时间； **是介于两者之间的，最好所有的都在这个约束内完成**
    * 超过期望时间是否仍允许执行；**允许**
    * 允许迟到时，最晚可以等待到什么时候；**SLA的20%**
    * 迟到惩罚是线性、分段还是其他形式。**线性**
    
    建议将Soft任务拆分为：
    
    * latency_limit：最好都在这个约束内完成
    * `latest_latency_limit`：可接受的绝对上限。

**[待确认]-[已确认]** 训练阶段采用等权归一化、Pareto多策略、约束优化。其他方法留到后续算法设计阶段决定。



**[待确认]** 训练时主要优化覆盖、消纳、绝对绿电使用量，还是组合指标。评估报告应至少分别展示覆盖与消纳，不能混为一个指标。

先按照组合指标优化，后续考虑深入优化。



#### v0.2（待评审）  文档中不太理解的部分

##### 4.5 传输时间计算   --我不理解

**[待确认]** 需要统一 `data_size`、`bandwidth_demand`、路径容量和传播时延之间的关系。

目标模型应满足：

* 任务在传输区间内占用路径每条链路的 `bandwidth_demand`；
* 传输时长由数据量、实际预留带宽和路径传播时延共同决定；
* 不能一边按任务带宽预约，一边按链路全部容量计算传输时长。

具体公式需要在需求评审中确认。

##### 5.4 DQN的阶段性定位  -- 需要设计思路

**[待设计]** 当前固定动作空间“计算节点 + WAIT”无法直接表达可变数量的“节点—未来时间”候选。后续必须重新设计动作表达或候选压缩机制，但这属于需求确认后的算法设计工作。

##### 8.4 None SLA    -- 不太理解

**[待确认]** None任务没有业务启动截止时间，但必须受系统最大预约窗口约束，不能无限搜索未来时间。

需要定义：

* 最大预约范围 `MAX_RESERVATION_HORIZON`；
* 窗口内没有候选时的处理方式；
* None任务是否允许进入外部批处理队列。


