# v1.0实施状态总览

版本：v1.0  
状态：需求基线已冻结；v1.0运行实现已完成；正式多种子baseline尚未重跑  
日期：2026-07-18

## 1. 当前结论

系统默认入口已经从旧调度机制切换到v1.0：

- `config.py`中的`SCHEDULER_ENGINE="v1"`；
- `train.py`转入`train_v1.py`；
- `evaluate.py`转入`evaluate_v1.py`；
- 正式候选模式固定为`complete`；
- 任务、候选、模型、指标和聚合schema均为`1.0`。

旧实现仍保留为历史兼容和回归对照，但不是默认运行路径。旧5种子baseline及其CSV、metadata未修改，仍只能作为旧机制诊断结果。

## 2. 已完成工作包

| 工作包 | 状态 | 主要结果 |
| --- | --- | --- |
| WP1 领域基础 | 完成 | 时间单位、不可变TaskSpec、SLA类型、旧None迁移、字段校验 |
| WP2 资源与事件 | 完成 | JIT传输、半开区间、真实峰值日历、CPU/BW原子预约、生命周期事件 |
| WP3 在线调度 | 完成 | 固定队列顺序、完整候选枚举、Pending事件重搜、逐任务提交 |
| WP4 目标与策略 | 完成 | 最早/最低成本/最高绿能/等权策略、Pareto诊断、独立近似模式 |
| WP5 Candidate-DQN | 完成 | 可变候选共享Q网络、18维候选特征、Double-DQN、物理时间折扣 |
| WP6 物理核算 | 完成 | 全区间能量/成本积分、无顺序绿电归因、估计与实现分账 |
| WP7 正式评估 | 完成 | 三阶段结算、逐任务/逐决策记录、显式N/A、配对统计和质量门槛 |
| 验收与审计 | 完成 | 50项需求、258个目标测试ID、19个不变量均有自动追踪证据 |

## 3. 验证结果

- Python编译检查通过；
- `python -m unittest discover -v -s . -p "test_*.py"`：202项全部通过；
- v1.0正式评估入口冒烟测试：`VALID`；
- Candidate-DQN训练入口零步冒烟测试：模型成功保存并通过metadata约束；
- 追踪审计：R-01至R-50共50项、正式目标测试ID共258个、I-01至I-19共19个，缺失均为0；
- 正式JSON拒绝NaN/Infinity，版本或特征hash不兼容时失败关闭。

## 4. 尚未执行的实验工作

以下内容不是代码实现缺口，而是下一阶段实验任务：

1. 通过pilot确定Soft/Flexible迟到权重、学习率、网络规模和正式样本量；
2. 使用同一冻结任务轨迹和外生轨迹运行不少于10个配对种子；
3. 重新生成v1.0正式baseline；
4. 执行成本、绿能、负载、完成率、主动等待和近似候选消融；
5. 根据预注册置信区间及工程门槛给出PASS/FAIL结论。

## 5. 入口文件

- 业务与算法说明：`当前系统详细说明_v1.0.md`
- 冻结需求：`需求规格说明_v1.0.md`
- 算法设计：`调度算法设计_v1.0.md`
- 验收规格：`验收测试规格_v1.0.md`
- 代码追踪审计：`traceability_audit.py`
- 正式评估：`evaluate_v1.py`
- 正式训练：`train_v1.py`
- 训练规模与目标参数pilot：`train_v1.py --preflight-only`、`calibrate_v1_objective.py`
- 配对模型效果分析：`analyze_v1.py`
- 训练与评估操作步骤：`训练与效果评估操作指南_v1.0.md`
